---
const now = new Date()

const MY_TIMEZONE = "America/Chicago"
const time = new Intl.DateTimeFormat(undefined, { hour: 'numeric', minute: '2-digit', timeZone: MY_TIMEZONE }).format(now)
---

<span class="localtime">
    <time class="my-time">{time}</time> for me
</span>

<style>
    .localtime {
        white-space: nowrap;
    }
</style>
